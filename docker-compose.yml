services:
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_DB: sptts
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: 2002
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 5

  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://host.docker.internal:5432/sptts
      SPRING_DATASOURCE_USERNAME: postgres
      SPRING_DATASOURCE_PASSWORD: 2002

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

# volumes:
#   postgres_data:
